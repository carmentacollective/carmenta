# Worker Library

Background worker utilities including system prompts for scheduled jobs.

@.cursor/rules/prompt-engineering.mdc

**Before editing `system-prompt.ts`, invoke the `writing-for-llms` skill.**
