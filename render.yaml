# This file is NOT used - service is configured manually in Render dashboard
#
# Configuration:
# - Build Command: ./scripts/render-deploy.sh
# - Start Command: bun .next/standalone/server.js
# - Runtime: Bun (auto-detected from bun.lock and .bun-version)
# - BUN_VERSION env var: 1.3.3
