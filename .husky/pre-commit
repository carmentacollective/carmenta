#!/bin/sh
# Run lint-staged to check and fix files before committing
echo "🔍 Running pre-commit checks..."
~/.bun/bin/bunx lint-staged || {
  echo "❌ Pre-commit checks failed. Fix errors above and try again."
  exit 1
}
echo "✅ Pre-commit checks passed"
