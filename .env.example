# OpenRouter API Key
# Get yours at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Sentry Configuration
# Get your DSN from https://sentry.io - create a Next.js project
# Sentry is enabled automatically in production (NODE_ENV=production)
SENTRY_DSN=your_sentry_dsn_here
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here

# Optional: Auth token for source map uploads (CI/CD)
# Create at https://sentry.io/settings/auth-tokens/
# SENTRY_AUTH_TOKEN=your_sentry_auth_token_here

# Clerk Authentication (Optional for development)
# The app gracefully handles missing Clerk keys:
# - Without keys: App runs, but auth-protected features (/connect API) will fail
# - With keys: Full authentication enabled
#
# This supports git worktrees, forks, and local development without auth setup.
#
# To enable authentication:
# 1. Create a new application at https://dashboard.clerk.com/apps/new
# 2. Enable Email and Google OAuth in "User & Authentication" > "Email, Phone, Username"
# 3. Copy the API keys from "API Keys" section and set them below
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here

# Clerk redirect URLs (use app pages instead of Account Portal)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Parallel Web Systems API Key
# Get yours at https://parallel.ai
PARALLEL_API_KEY=your_parallel_api_key_here

# E2E Testing with Clerk Testing Tokens
# Required for running authenticated Playwright tests
# @see https://clerk.com/docs/testing/playwright/overview
#
# Create a test user in your Clerk dashboard for e2e tests:
# 1. Go to Clerk dashboard > Users > Create user
# 2. Use email/password authentication (not OAuth)
# 3. Set the credentials below
E2E_CLERK_USER_EMAIL=your_test_user@example.com
E2E_CLERK_USER_PASSWORD=your_test_password_here
